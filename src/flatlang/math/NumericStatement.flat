package "flatlang/math"

class NumericStatement extends Statement {
  NumericTree tree

  public construct(String statement) {
    tree = NumericTree(statement.trim())
  }

  public toString() => "#tree"
}